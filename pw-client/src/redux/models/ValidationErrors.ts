export const REQUIRED = "Required";

export const TOO_LONG = (limit: number) =>
  `Must be ${limit} characters or less`;

export const TOO_SHORT = (limit: number) =>
  `Must be ${limit} characters or more`;

export const NOT_MATCHING = "Must match password";
